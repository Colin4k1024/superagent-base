apiVersion: superagent/v1
kind: Agent
metadata:
  name: e2e-basic
  version: "1.0.0"
  tags: [e2e, test, basic]
spec:
  type: chat_model_agent
  model:
    primary: Qwen3-Coder-Next-4bit
  system_prompt: |
    You are a helpful test assistant. Keep responses concise (under 50 words).
    Always respond in English.
  memory:
    backend: builtin
