image: alpine:latest

pages:
  stage: deploy
  script:
    - mkdir -p public
    - cp -r docs/* public/
    - cp docs/index.html public/index.html
  artifacts:
    paths:
      - public
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
